[
    {
        "name": "By points",
        "query": "SELECT id, SUM(points) AS Points FROM (SELECT participants.red_a AS id, results[1].red_a.points AS points FROM results UNION ALL SELECT participants.red_b AS id, results[1].red_b.points AS points FROM results) GROUP BY id ORDER BY Points DESC;"
    }
]
